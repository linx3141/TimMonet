# 更新日志

## 自 1.1（tag `2-1.1`）以来的更新 · 2026-09-07 → 2026-09-19

> 这一版主要是"深色下看不见的东西"和"颜色不对/割裂"的修复，共 30 多项改动。

### 一、深色下原本看不见的，现在都看得见了

- **支付密码弹窗**：6 个密码格原本是纯白；输入的圆点、右上角关闭 ×、删除键也几乎看不见。现在整块跟随主题（格子变成深色面、圆点用主色、图标可见）。
- **语音 / 视频通话界面**：麦克风、免提、蓝牙、挂断等按钮**图标和它自己的底同色**，等于隐形 —— 已修；通话菜单里的图标同样修复。
- **转发弹窗的「输入留言」框**：转发普通文字时它和弹窗完全同色，根本看不出是个输入框 —— 现在是一块比弹窗略浅的输入框底（转发小程序卡片时也一致）。
- **资料卡「加好友」按钮**：原本和页面同色，现在是一块浅一号的"面"，按下去会变深（有按压反馈），"发消息"仍是主色按钮。
- **引用条**：右上角跳转箭头、引用区里的图标与链接现在跟随引用文字配色，不再消失或发灰；「取消引用」的圆底按钮改成主色圆底 + 对比色叉号。
- **首页**：打开菜单时的压暗遮罩不再变成一块实心亮片；搜索栏的放大镜图标和「搜索」占位文字不再看不见。
- **文件页「微云」入口条**：深色下不再是一条刺眼的亮条；提示条图标已修。

### 二、颜色更贴近 TIM 原本的明暗关系

- **顶栏 / 状态栏 / 二级导航栏**（例如"发送给 / 关闭 / 多选"那一整条）：恢复成"比页面亮一档的面"，不再比内容暗、与搜索栏和卡片割裂。
- **联系人页**：「新朋友 / 群通知」和整个通讯录列表（特别关心、我的好友…）改为**与页面同色**，不再整片浮成卡片。
- **设置页**：列表圆角恢复正常，不再每一行都变成独立的全圆角卡片。
- **资料卡（个人资料页）**：卡面与页面底重新分开，卡片边界回来了。
- **QQ 钱包页**：补齐染色，不再是大片白底 + 品牌蓝顶栏。
- **群公告（H5）**：列表卡片与页面底不再糊成一个颜色。
- **气泡**：气泡里的图标与链接色跟随气泡文字色。
- **未读角标**分色显示；红包等图标不再被当成"小红点"染错。
- **极光卡片 / 设置卡片**：不再偏暗。
- 第三方模块注入的界面（例如 QAuxiliary 的设置页）不再被误染色。

### 三、更稳、更省电

- **群公告页不再卡死**（注入脚本自己触发自己、无限循环的问题已修）。
- **联系人页下拉时顶栏下方的白线消失了**（包括更早期那条 1px 细线）。
- 不再把**彩色图标、照片类背景**误当成纯色块刷掉。
- 壁纸 / 设置变化后颜色立即生效；跟随系统深浅色切换更准。
- 热路径（每帧、每次取色都会走的代码）做了收敛与缓存，长期使用更省电、更少卡顿。

### 四、使用上的变化

- **模块设置入口加进了 TIM 自己的设置页**，面板在 TIM 进程内打开，改完即时预览。
- 修改**配色相关**设置后，TIM 会**自动重启一次**（这是为了让底栏、顶栏这类已经画好的区域也按新配色重建）。不涉及配色的开关不需要重启。

---

<details>
<summary>给维护者：这一版的内部改动（点开）</summary>

- 判据统一：亮度/彩度、角色色、深浅判定全部收敛到 `BgResolver` / `MonetPalette`；清掉"按颜色相近猜"的旧逻辑与 `dark` 假形参（含 64 个调用点）。
- 移除几何/时间兜底：圆角不再由几何覆盖（改由 TIM 数据层决定）、支付密码不再用"120ms 时间邻接"、提示条按类名判定。
- 资源层补 `setBackgroundDrawable` 入口（资料卡卡面就漏在这里）；span 层（引用条不是 View）改从写入入口限定。
- 死代码清理（净删约 300 行）：`ThemeState.kt`、`palette(dark)` 重载、零调用 API、22 个未用形参、4 条未用字符串、未用 import 等。
- `ArkPackagePatcher` 接上从未生效的 `blockRegex`（升版本时剥旧补丁块），并把"改注入内容必须升 MARKER 版本号"写进文档。
- 文档：AGENTS.md 补坑 27–30，公开 3 份审计清单。

</details>
